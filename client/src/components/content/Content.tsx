import React from 'react';

/**
 * Здесь должен быть контент, который будет доступен и недоступен пользователю,
 * в зависимости от его состояния, блокирован он или нет, авторизован или нет)
 * @constructor
 */
const Content = () => {
    return (
        <div>
            asasd
        </div>
    );
};

export default Content;
